{
		"version": "0.2.0",
		"configurations": [
			{
				"name": "Launch",
				"type": "go",
				"request": "launch",
				"mode": "debug",
				"program": "${fileDirname}",
				"env": {
					"GOPATH": "${workspaceRoot}"
				},
        "args": ["-gcflags=all=-N -l"]
			}
		]
	}